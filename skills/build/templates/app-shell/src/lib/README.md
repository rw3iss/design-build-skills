# lib/

Pure utilities. **No Preact imports allowed** — if you need hooks or effects, put it in `hooks/` or `services/` instead. Examples: formatters, parsers, math helpers, validators, type guards.

Easy to test, easy to tree-shake, easy to share across projects.
