import type { JSX } from 'preact';
import './App.scss';

export function App(): JSX.Element {
	return (
		<main class="app">
			<h1>__APP_TITLE__</h1>
			<p>Scaffold ready. Start building components under <code>src/components/</code>.</p>
		</main>
	);
}
